# URL base del backend. En desarrollo local podés dejarla vacía: App.jsx
# cae a http://localhost:8080 por defecto. En un build de produccion
# (npm run build / Dockerfile) hay que fijarla a la URL real del backend.
VITE_API_URL=http://localhost:8080
